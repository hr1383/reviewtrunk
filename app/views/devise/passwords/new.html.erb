<div class="login_sctions">
	<h1 style="margin-top: 0px;"><span>Send me reset password instructions</span></h1>
    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= devise_error_messages! %>
     	<div class="row">
     		<div class="col-sm-10 col-sm-offset-1">
                <%= f.email_field :email, autofocus: true, placeholder: "Email"%>
     		</div>
     	</div>
     	
        <p class="submit"><%= f.submit "Send me", class: "button button_btn blue" %></p>
     	<!-- <button class="button button_btn blue" type="submit"><span>Sign in to your account</span></button></p> -->
    <%end%>
</div>
<div class="row">
	<div class="col-sm-9 col-sm-offset-3">
          	<p class="devise-shared-links"><%= render "devise/shared/links" %></p></a>
  	</div>
</div>

